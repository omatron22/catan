import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Note: If 'next build' fails due to '!' in directory name (webpack limitation),
  // either rename the project directory or use: NEXT_BUILD_TURBOPACK=1 next build
};

export default nextConfig;
